name: serverpod_app_server
description: Starting point for a Serverpod server.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '<4.0.0'

dependencies:
  serverpod: ^1.0.0

dev_dependencies:
  lints: ^4.0.0
